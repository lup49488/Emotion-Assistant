# Compatibility entry point for a production/runtime installation.
#
# Use requirements/dev.txt for tests and requirements/offline.txt for the
# optional corpus-analysis scripts. The split keeps deployment images free of
# tools that are not needed to serve the application.
-r requirements/runtime.txt
